pre-commit:
  parallel: true
  commands:
    lint:
      run: bun run lint
    format:
      run: bun x oxfmt --check "src/**/*.{ts,tsx}"
    typecheck:
      run: bun run typecheck

pre-push:
  commands:
    test:
      run: bun run test
